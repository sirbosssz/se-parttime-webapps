import React, { Component } from 'react';

export default class DetailsSection extends Component {
    render() {
        return(
            <section id="detailssection-guest">
                Details Section
            </section>
        )
    }
}